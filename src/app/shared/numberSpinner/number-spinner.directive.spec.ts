import { NumberSpinnerDirective } from './number-spinner.directive';

describe('NumberSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
